const d=getData(),list=document.getElementById('reports'),area=document.getElementById('pdfArea');function build(a){const subjects=['Reading','Grammar','Dictation','Phonics','Writing','Homework','Classwork','Commitment'];const body=a.rows.map((r,i)=>'<tr><td>'+String(i+1)+'</td><td style="text-align:left">'+esc(r.name)+'</td>'+r.marks.map(x=>'<td>'+ (x?'✓':'') +'</td>').join('')+'<td><b>'+esc(r.average)+'</b></td><td>'+esc(r.notes||'')+'</td></tr>').join('');const averages=subjects.map((_,j)=>{const n=a.rows.reduce((s,r)=>s+(r.marks[j]?1:0),0);return Math.round(n/a.rows.length*100)+'%' }).join('</td><td>');return '<div class="pdf-sheet" id="sheet"><div class="pdf-head"><div class="pdf-school">Halley International School – HIS</div><div class="pdf-title">Weekly Student Assessment – '+esc(a.className)+'</div></div><div class="pdf-info"><div><b>Week:</b> '+esc(a.week)+'</div><div><b>From:</b> '+esc(a.from||'—')+' &nbsp; <b>To:</b> '+esc(a.to||'—')+'</div><div><b>Teacher Name:</b> '+esc(a.teacher)+'</div></div><div class="pdf-scale"><b>Rating Scale:</b> 5 = Excellent &nbsp; 4 = Very Good &nbsp; 3 = Good &nbsp; 2 = Fair &nbsp; 1 = Needs Improvement</div><table class="pdf-table"><thead><tr><th>No.</th><th>Student Name</th>'+subjects.map(s=>'<th>'+s+'</th>').join('')+'<th>Average</th><th>Notes</th></tr></thead><tbody>'+body+'</tbody><tfoot><tr><td colspan="2"><b>Class Average</b></td><td>'+averages+'</td><td></td><td></td></tr></tfoot></table></div>'}function showPdf(a){area.innerHTML=build(a);area.style.position='static';window.scrollTo(0,0)}list.innerHTML=d.assessments.length?d.assessments.slice().reverse().map(a=>'<div class="report-item"><div><strong>'+esc(a.className)+' — Week '+esc(a.week)+'</strong><small>'+esc(a.teacher)+' · '+esc(a.from||'')+'</small></div><div><button class="btn ghost" onclick="showPdf(getData().assessments.find(x=>x.id=='+a.id+'))">Preview</button> <button class="btn primary" onclick="downloadPdf(getData().assessments.find(x=>x.id=='+a.id+'))">PDF</button></div></div>').join(''):'<p class="hint">No saved assessments yet.</p>';async function downloadPdf(a){showPdf(a);const el=document.getElementById('sheet');await html2pdf().set({margin:0,filename:'HIS_'+a.className+'_Week_'+a.week+'.pdf',image:{type:'jpeg',quality:.98},html2canvas:{scale:2,useCORS:true},jsPDF:{unit:'mm',format:'a4',orientation:'landscape'}}).from(el).save();area.style.position='absolute'} 